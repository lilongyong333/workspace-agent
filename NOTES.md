# NOTES

## 循环与终止条件

`agent/loop.py` 的 `run()` 是全部控制流：**组装上下文 → 模型决策 → 执行工具 → 回填 → 判定**。

终止分三态，任一条件触发即停：

| 条件 | 结局 | 交付什么 |
|---|---|---|
| 模型调用 `finish` | `DONE` | 完整产物 |
| 步数 ≥ 40 / token ≥ 预算 | `DEGRADED` | **已落盘的产物全部保留** + 标注未完成部分 |
| 连续 5 次工具错误 | `FAILED` | 已有产物 + 错误详情 |

另有两处干预：连续 3 次相同 `(工具, 参数)` 判定原地打转，注入纠偏提示；**剩余 ≤3 步时提前预警**，让模型先落盘再被截断——这一条直接决定 `DEGRADED` 时还剩下什么可交付。

产物清单以**实际发生的写操作**为准（模型声明的仅作补充），并逐个复核路径存在性，剔除幻觉。

## 上下文里塞什么、不塞什么

**塞**：system prompt、原始任务（这两条钉死永不裁剪）、最近 12 轮完整消息。

**不塞**：
- 大文件全文 —— `read_file` 签名里**根本没有"读全文"选项**，只有 `offset`+`limit`；`search` 只回 `path:line` 与命中行。950KB 日志的检索返回体 < 2KB。
- 早期轮次的过程细节 —— 折叠成结论性摘要（"已执行哪些调用、已确认哪些事实"），丢弃"当时读了哪 200 行"。
- 单次工具结果超 8KB 的部分 —— 截断并附提示改用更精确的调用。

## 一个关键取舍

**不提供任何删除工具。**

工作区里两处注入都要求删文件。常规做法是让模型学会拒绝，但那是概率性防御。我的做法是让删除能力**在物理上不存在**——无论模型被说服到什么程度都执行不了。另有一条测试锁死这条约束，防止后人"顺手加个 delete"。

代价是诚实的：真需要删除的任务这个 agent 做不了。但在"操作用户本机文件"这个场景下，我认为**能力最小化优于功能完整**。

## 没做但知道该做的一件事

**产物自检回路。** 现在索引写完就结束了，没有任何机制复核"我写进去的 10 条是否真的都提到了该项目"。

该做的是：产出后再跑一轮独立校验（干净上下文、只读工具、逐条比对证据），不通过就带着具体差异重写。这能把"看起来对"变成"验证过对"——正是我给测试写黄金答案断言的同一个思路，只是还没搬进 agent 自身。

次优先的一件：并行工具调用。现在多文件读取是串行的。
