# =============================================================
# 注意：.gitignore 的 # 只有在【行首】才是注释。
# 写成 `/workspace/   # 说明` 会让整行（含空格与说明文字）
# 变成模式本身，结果是什么都匹配不到 —— 而且 git 不会报错，
# 只会在你 commit 之后才发现产物全被提交了。
# 所以本文件的所有注释都独占一行。
# =============================================================

# -------- 密钥：绝不入库 --------
.env
.env.*
!.env.example

# -------- Python --------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# -------- 运行期产物 --------
# Web 端每会话的独立工作副本
/sessions/
# 本地跑 CLI 时的可写工作目录（只读种子是 workspace_seed/）
/workspace/
# 轨迹输出（trace.jsonl / trace-t2.jsonl / 任何 *.jsonl 轨迹）
trace*.jsonl
.artifacts/
# 索引库。里面是被索引文件夹的**正文内容**，可能是任意私人/公司文件，
# 属于最不该进公开仓库的东西。索引随时可由源文件重建，没有保留价值。
/.index/
*.db-wal
*.db-shm
# ask --out 的落点。内容是私有语料的衍生结果，同样不该进公开仓库。
/answers/

# -------- 编辑器 / 系统 --------
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
