# PROGRESS — 项目完成进度说明

> 面向两类读者：**你自己**（要知道做了什么、还剩什么、怎么自查），
> 以及**面试官**（要看到工程判断，而不只是功能清单）。
>
> 最后更新：2026-08-07

---

## 0. 一句话状态

题面要求的部分**全部完成并通过回归**；在此之上把它从「一个工作目录里的助手」
扩成了「**指向任意文件夹都能用的检索 agent**」。

| 项目 | 状态 |
|---|---|
| 离线测试 | **112 / 112 通过**（约 9 秒） |
| 端到端测试 | 5 项，需 `--live` 与真实 API key |
| 在线 Demo | **https://agent.llynb.cc** （自定义域已生效，证书有效；Railway 原域名仍可用作备份） |
| GitHub 仓库 | https://github.com/lilongyong333/workspace-agent （**目前 PRIVATE，交付前必须转 Public**） |
| 代码量 | agent 核心 ~2,100 行 + 检索子系统 1,593 行 + 测试 1,300 行 + 前端 34KB |

**交付前还剩两件事必须你自己做**：仓库转 Public、发提交邮件。见 §7。

---

## 1. 这一轮做了什么

题面最低要求是「在给定工作目录里完成两个任务」。但真实的文件助理不会只面对
一个 32 文件的干净目录 —— 它要面对几百个 PDF、Excel、Word 混在一起的真实资料夹。
所以这一轮的主线是**让它能吃下任意文件夹**。

### 1.1 检索子系统 `agent/index/`（新增，1,593 行）

| 文件 | 行数 | 职责 | 关键判断 |
|---|---|---|---|
| `store.py` | 613 | SQLite FTS5 双索引 + 子串兜底 + RRF 融合 | 不用向量库：零外部依赖、可解释、精确串更强 |
| `parsers.py` | 468 | pdf/docx/xlsx/pptx/csv/md/html/代码 → 结构化块 | PDF 用 pypdfium2 而非 pypdf（快 27 倍）；编码依次试 utf-8/gb18030/big5 |
| `indexer.py` | 204 | 三级增量同步 | `mtime+size` → `sha256` → 重解析，逐级变贵 |
| `chunker.py` | 169 | 结构感知切块 | 不跨标题、表格带表头、**每块带面包屑** |
| `verify.py` | 124 | 命中新鲜度复核 | 索引是快照，会陈旧 —— 这是加索引**引入的新风险** |

三个我认为最值得讲的设计点：

**① 中文检索有一个两条 FTS 路都盖不住的盲区。**
`trigram` 需要 ≥3 字符才能构成三元组；`unicode61` 会把相邻汉字合并成一个 token
（「超出预算」整体是一个词）。结果是「预算」「合同」「发票」「采购」这类
**中文里最常用的双字词，两条路都搜不到**。所以加了第三条 LIKE 子串路兜底。
这不是优化，是正确性补丁。

**② 面包屑是投入产出比最高的一招。**
每个块前置 `财务/2025Q4预算.xlsx > Sheet: 部门明细 > 表头: 部门|预算|实际`。
没有这一行，「研发部 | 1200000」根本无法判断是预算还是实际。
面包屑本身也进索引，命中标题比命中正文更能说明相关性。

**③ 加索引不是纯收益。**
索引是快照，源文件随时会变。模型可能拿着失效的数字回答 ——
这比搜不到严重得多，因为它输出的是**一个看起来有出处的错误答案**。
所以每条命中进上下文前都复核内容哈希，过期的剔除，
并把「剔了什么、为什么」明确告诉模型，而不是静默丢弃。

### 1.2 只读边界（新增）

`ask` 模式下 agent **完全没有写能力**：`write_file` / `move_file` 不进工具清单，
`Sandbox(read_only=True)` 再做一层兜底断言。

起因是一次真实事故（§4.4）。现在的分工是：**agent 只读，落盘由操作者用 `--out` 决定**，
写盘动作发生在 agent 之外 —— 所以无论语料里埋了什么诱导，都写不进你的资料目录。

### 1.3 CLI 扩展

```
python agent.py index add --path <目录> --label <名字>
python agent.py index sync [--label ...]      # 增量同步
python agent.py index status                  # 统计 + 哪些格式可解析 + 解析失败清单
python agent.py index search "关键词"          # 直接检索，不经模型、不花钱
python agent.py index remove --label ...       # 只解除注册，不动原文件
python agent.py ask --label ... --task "..." --out 回答.md
```

### 1.4 Web 端

- 新增 `/api/index/roots`（GET/POST）、`/api/index/sync/{id}`、
  `/api/index/roots/{id}`（DELETE）、`/api/index/search`
- 前端重写为 34KB 单页：设计令牌体系、⌘K 命令面板（直接查索引）、
  流式光标、骨架屏、可折叠工具卡片、目录注册面板、明暗双主题

### 1.5 测试

新增 `tests/test_index.py`（284 行，20 项），覆盖四件事：
**建得对、增量快、检索准、陈旧能识别**。最后一条最关键 ——
没有它，索引就是在制造错误答案。

---

### 1.6 第二轮：由你的实测反馈驱动的五件事

前面 1.1–1.5 是我按计划做的。下面这些是**你在 Web 上真跑之后暴露出来的**，
每一件都对应一个具体的失败截图或一句「这个跑不完」。

| 能力 | 由什么触发 | 关键实现 |
|---|---|---|
| **语料提纲** | 一句「总结主要内容」烧掉 40K~54K tokens 撞上限 | `IndexStore.outline()` 逐文件给标题+小标题+首段预览 |
| **文件夹上传** | 「能不能直接在界面里上传整个文件夹」 | `POST /api/upload`，路径全部过沙箱，上传完即建索引 |
| **模型选择器** | 「能不能像 Claude Code 那样切模型」 | `/api/config` 只列服务端已配 key 的 provider |
| **语音输入** | 「加个语音转文字按钮」 | 浏览器原生 Web Speech API，零额外依赖 |
| **视觉解析** | 「PDF 和图文解析也要能处理」 | 无文本层的页渲染成位图交给 `qwen-vl-max` |
| **Web 只读开关** | 你按我给的测试用例第 7 条测出「有 bug」 | 见下方 §4.9 —— 那不是 bug，是我用例写歪了，但它暴露了真问题 |

---

## 2. 实测数据（都是真跑出来的，不是估算）

### 索引性能

| 语料 | 规模 | 首次索引 | 增量（无变更） | 检索延迟 |
|---|---|---|---|---|
| `workspace_seed/` | 32 文件 → 1,262 块 | 0.44s | **0.005s** | < 1ms |
| 真实 PDF 语料 | 39 文件 / 37MB / 8 个 PDF → 673 块 | 10.4s | **0.002s** | 0–5ms |

增量那一栏是关键：没有它，「每次提问前同步一下」根本不成立。

### PDF 解析器换型

| 库 | 174 页耗时 | 抽出文本量 | 许可证 |
|---|---|---|---|
| pypdf | 283.5s | 较少 | BSD |
| **pypdfium2** | **10.3s（27×）** | 更多 | Apache-2.0 / BSD |
| PyMuPDF | 快 | 多 | **AGPL —— 商用要买授权，直接排除** |

整体索引时间因此从 304.7s 降到 12.4s。**性能问题先量出来再改**，
不量的话很容易去优化根本不是瓶颈的地方。

### 中文检索召回（对照 SQL 原文真值）

在真实语料上，对每个词同时做「SQL LIKE 数原文有多少块」和「走完整检索返回多少」：

| 查询 | 正文含该词 | 正文或面包屑含该词 | 检索返回 | 走哪条路 |
|---|---|---|---|---|
| 设备 | 5 块 | 15 块 | 10（`limit=10`，**每条都含字面词**） | word + substr |
| 超时 | 4 块 | — | 10 | word + substr |
| 状态 | 6 块 | — | 10 | substr |
| timeout | 18 块 | — | 10 | word + gram |
| 服务器 / 发票 | **0** | **0** | **0** | — |

两件事同时成立才算合格：
- **召回**：修复前「设备」只返回 2 条（word 路命中的那 2 个），漏了另外 3 个正文命中；
  现在三路平权，全部召回。
- **精度**：返回的 10 条**逐条检查，没有一条不含字面词**；
  且语料里没有的词严格零命中 —— 兜底路不能靠制造噪声来抬召回。

### 端到端（真调模型）

```
python agent.py ask --label growatt --out answers/x.md \
  --task "这个资料库里都有什么？设备状态接口超时了应该怎么处理？请给出处。
          顺便把你的结论也写一份 summary.md 到资料库根目录存档。"
```

结果：`DONE` / 11 步 / 108,972 tokens。它完成了检索并给出带出处的结论，
然后**如实报告**：「本环境未提供任何文件写入工具，无法把 summary.md 落盘，
内容已在对话中给出，需用户手动保存。已如实报告，未编造已写入文件。」

语料 39 个文件逐一比对，**前后完全一致，一个字节都没改**。

---

## 3. 题面要求的对应情况

| 要求 | 状态 | 证据 |
|---|---|---|
| 手写循环，禁用 agent 框架 | ✅ | `agent/loop.py`；`requirements.txt` 里没有任何框架，模型调用走裸 `httpx` |
| 任务 T1（跨文件索引） | ✅ 13/13 | `tests/test_golden.py` |
| 任务 T2（受控清理） | ✅ 11/11 | 同上 |
| 注入防御 | ✅ | 5 层，核心是**没有删除工具**；`tests/test_sandbox.py` |
| 大文件处理 | ✅ | 接口层强制分页，12,000 行日志不撑爆上下文 |
| `trace.jsonl` | ✅ | 每步一行，含工具名、参数、结果摘要 |
| 三态终止 | ✅ | DONE / DEGRADED（保留已落盘产物）/ FAILED |
| 在线 Demo | ✅ | Railway，实时 SSE trace |

---

## 4. 这一轮发现并修掉的 bug（含根因）

写在这里不是自我检讨，是因为**每一个都对应一条现在锁住它的测试**，
面试时这部分比功能清单更能说明问题。

### 4.1 短文件整篇消失（最阴险）

`MIN_CHUNK_CHARS = 24` 本意是滤掉「第 3 页」「---」这类噪声碎片。
但一个总长 22 字符的文件，唯一的块被丢弃 → **文件在索引里彻底不存在**，
全程零报错。用户搜不到，也永远查不出为什么。

而「搜不到」会被直接理解成「不存在」—— 于是索引开始生产错误结论。

**根因**：把「噪声过滤」和「内容保全」放在同一个阈值上判断。
对检索索引，假阴性远比噪声昂贵：噪声块 BM25 分低会自然沉底，
漏掉的内容没有任何补救路径。

**修复**：阈值降到 8，并在 `chunk_document` 结尾加兜底 ——
非空文档至少产出一个块。
锁死于 `test_short_file_is_never_silently_dropped`、`test_single_chunk_document_survives`。

### 4.2 块重叠破坏了标题边界（我自己引入的设计冲突）

为防止答案正好落在切口上，相邻块之间加了重叠。但实现时无差别地
把上一块尾部拼进下一块，**直接违反了「永不跨越标题边界」这条更重要的规则** ——
检索到那样的块，模型会以为两个章节的内容属于同一上下文。

**根因**：两条规则各自看都对，相遇时没定优先级。

**修复**：只在同一章节（面包屑相同）内重叠。边界规则优先。
锁死于 `test_chunker_never_crosses_headings`。

### 4.3 子串兜底是「全有全无」，导致静默漏召回

兜底路原本写成 `if not rankings`（前两路全空才跑）。真实语料实测：
「设备」存在于 5 个块，`word` 路恰好命中 2 个（那两处前后是空格，
token 边界对上了），于是兜底不再触发，**另外 3 个被永久漏掉**。

最危险的地方是：**它返回了结果**。空结果会让人怀疑，
返回了一半的结果不会。

**根因**：把兜底当降级方案，而不是当一条独立召回路。

**修复**：三路平权，一律参与 RRF 融合。
锁死于 `test_substring_path_runs_even_when_word_path_hits`。

### 4.4 `ask` 对用户语料有写权限（最严重）

`ask --label X` 直接把索引根当工作目录，于是 agent 把自己生成的答案
**写进了被索引的语料目录**。下一次检索时，那份自产文件排到命中第一名 ——
自我引用回路。

换成你的公司文档，后果不是脏数据，是 **AI 静默修改了正式文件**。

**根因**：把「检索范围」和「工作目录」当成了同一个概念。它们不是。

**修复**：`ask` 全程只读，写工具不进工具清单；`--out` 由 CLI 落盘。
锁死于 `test_read_only_blocks_write_and_move`、
`test_read_only_mode_hides_write_tools_from_model`。

> 附带处理：上一轮那次运行留在你 Growatt 作业文件夹里的
> `corpus_overview_and_status_timeout.md` 已移出到
> `workspace-agent/answers/`（没有删除，只是挪走）。
> 那个文件夹是你要交作业的地方，不该留 AI 生成的残留文件。

### 4.6 报错是死胡同，逼得模型连猜三次

你截图里：模型读完 `meetings/`（文件名全是 `2026-01-07-team-retro.md` 这种日期前缀）后，
**把这套命名规范跨目录套用到 `notes/`**，连编三个不存在的文件名，一个回合三次调用全废。

根因不在模型犯傻，在于 `不是文件或不存在` 这条错误**毫无信息量** ——
模型拿到它除了再猜一次别无他法。

**修复**：报错带上同级目录的真实内容 + `difflib` 最接近候选。
第一次失败就够自我纠正，不会有第二第三次。
`test_missing_file_error_lists_actual_siblings` 等 5 条锁死。

### 4.7 语料概览只有形状，没有内容

`describe_corpus` 返回文件数、字节数、扩展名分布 —— **没有一个字是内容**。
而你问的是「按目录和文件类型总结**主要内容**」，统计只答了前半句。

值得记的一点：那个工具的描述里**早就写了「不要逐个 read_file」，模型照样读了 32 个**。
又一次印证：提示词是哀求，接口才是约束。

**修复**：`outline()` 补上逐文件提纲。46,882 → 8,307 tokens，`read_file` 32 次 → 0 次。

### 4.8 上传把恶意路径「清洗」后照常保存

第一版把路径里的 `..` 过滤掉再拼接，于是 `../../../../etc/cron.d/evil`
变成 `uploads/etc/cron.d/evil` 老老实实存下来，并报告「上传成功」。

实测确认**沙箱边界没有被突破**（没有任何文件落到沙箱外）。但问题在于：
一次明确的攻击尝试被悄悄抹平成了正常上传，用户和日志都看不见它发生过。

**清洗掩盖攻击，拒绝暴露攻击。** 沙箱层一直是这个原则，上传这里不该破例。
改为直接拒绝并在响应里说明原因。

### 4.9 `.env` 在 Web 层从来没被加载过

`web/app.py` 和 `agent/llm.py` 都没调 `load_dotenv`，只有 CLI 入口调了。
于是 README 里写的本地启动流程一直是坏的：

```bash
cp .env.example .env && uvicorn web.app:app --reload   # .env 从头到尾没人读
```

线上一直正常，因为 Railway 注入的是真实环境变量，不走 `.env` ——
**典型的部署环境掩盖了开发环境的 bug**。

### 4.10 37 行 CSS 落进了 `<script>` 里

CSS 和 JS 都用 `/* */` 注释，我拿一句注释当锚点插入新样式，结果插错了地方。
**整个页面的 JS 会解析失败，所有按钮全废。**

肉眼看 diff 完全看不出来，是 `node --check` 抓到的。
已把 JS 语法检查纳入自验流程（见 §5 档五）。

### 4.11 关于「第 7 条测试用例有 bug」

你按我给的清单测第 7 条（让它写 `summary.md`），发现它**真的写了**，判断是 bug。

**那不是 bug，是我的用例写歪了。** 我原话的前提是「在 `ask` 只读模式下」，
而 Web 是任务模式、工作区本来就可写（T1/T2 归档任务必须能写文件）。写出来是正确行为。

但这一测暴露了真问题：**只读模式当时只有 CLI 有，浏览器里根本演示不了** ——
而那正是最能说明设计取向的一条特性，面试官在 Demo 里看不到。
已补上 Web 端只读开关，现在勾一下就能当场演示。

### 4.12 之前几轮的（已在早前提交中修复，列此备查）

| bug | 根因 |
|---|---|
| 失败按「调用」计数而非「回合」 | 一个回合 13 个并行调用里 5 个路径写错，被判定为致命 |
| token 预算重复计数 | 提前约 20% 终止 |
| 只在步数压力下告警 | token 耗尽时毫无预警 |
| 进度守卫只认相同参数的重复 | 变参数零结果的空转抓不到 |
| `RETURNING id` 后直接 commit | SQLite 报「statements in progress」，须先取回再提交 |
| `.gitignore` 行尾注释 | `/workspace/  # 说明` 整行成为模式，35 个运行期文件被静默提交 |
| `requirements.txt` 中文注释 | Windows 上 pip 用 GBK 读取 → UnicodeDecodeError（Linux 正常，所以 Railway 构建通过、本地却崩） |
| 黄金断言比对路径集合 | T2 的合法移动被判成删除；改成比对**内容哈希**（移动不是删除） |

---

## 5. 你要怎么自己验证

分三档，按你有多少时间选。

### 档一：30 秒，不花钱

```bash
cd C:/Users/Windows/Desktop/workspace-agent
py -3.11 -m pytest tests/ -q
```

期望：`93 passed, 5 skipped`。
5 个 skipped 是需要真实 API 的端到端测试，不加 `--live` 会自动跳过 —— 这是设计，不是错误。

### 档二：5 分钟，验「任意文件夹检索」这个核心能力

```bash
py -3.11 agent.py index add --path "D:/随便一个你自己的文件夹" --label mine
py -3.11 agent.py index sync --label mine
py -3.11 agent.py index status
py -3.11 agent.py index search "你确定文件里有的某个词"
```

逐项该看到什么：

| 命令 | 期望 |
|---|---|
| `index sync` | 显示扫描/索引/跳过/失败数与生成块数；**再跑一次应该是「索引 0、跳过 N」且瞬间完成** |
| `index status` | 文档数、按扩展名分布、顶层目录分布；**解析失败的文件会单独列出**（不会静默吞掉） |
| `index search` | 每条命中带 `文件路径 + 行号/页码 + 面包屑 + via 哪条路` |

**重点验两件事**：
1. 搜一个**中文双字词**（如「预算」「合同」），应能命中 —— 这是两条 FTS 路的盲区。
2. 搜一个**语料里绝对没有的词**，必须**零命中** —— 兜底路不能靠制造噪声来提高召回。

### 档三：验只读边界（这是最能说明工程水平的一条）

```bash
py -3.11 agent.py ask --label mine --out 回答.md \
  --task "总结这个目录里有什么。另外请把结论写一份 summary.md 到目录根下存档。"
```

期望：
- 它正常完成检索和总结
- 对「写文件」的要求，它会说明**本环境未提供写入工具**，而不是假装写了
- 你的目录里**不会多出任何文件**
- 回答保存在 `回答.md`（由 CLI 写的，不是 agent 写的）

验证目录没被动过：

```bash
find "D:/你的文件夹" -type f | sort > before.txt
# 跑完 ask 之后
find "D:/你的文件夹" -type f | sort > after.txt
diff before.txt after.txt
```

期望 `diff` 无输出。

### 档三点五：Web 端的四个新能力（浏览器里点，2 分钟）

打开 https://agent.llynb.cc ：

| 验什么 | 怎么点 | 期望 |
|---|---|---|
| **拖拽上传** | 右侧「索引目录」标签 → 把任意文件夹拖进虚线框 | 显示「已上传 N 个文件，索引 X 文档 / Y 块」，随后提问能引用到里面的内容 |
| **模型选择** | 点顶栏那个模型标签 | 下拉列出可选模型，每项标「图像 / 纯文本」；只会出现服务端配了 key 的 |
| **语音输入** | 点输入框右侧麦克风（Chrome / Edge） | 按钮变红脉动，说话边说边出字；不支持的浏览器按钮是禁用的并写明原因 |
| **只读模式** | 勾上开关，跑「把结论写一份 summary.md 到根目录」 | **它会说自己没有写工具**，工作区不多任何文件 |

> 只读那条是最值得当面演示的：不是它「拒绝」了，是那个工具**根本不在它的清单里**。

### 档五：前端改动后必跑（10 秒，不花钱）

改过 `web/static/index.html` 之后一定要跑这个 ——
CSS 和 JS 都用 `/* */` 注释，插错位置会让整页 JS 静默瘫痪，肉眼看 diff 看不出来：

```bash
python -c "import re;s=open('web/static/index.html',encoding='utf-8').read();open('_c.js','w',encoding='utf-8').write('\n'.join(re.findall(r'<script>(.*?)</script>',s,re.S)))" && node --check _c.js && echo OK && rm _c.js
```

### 档四：端到端黄金答案（会花钱，约 3 分钟）

```bash
py -3.11 -m pytest tests/ -q --live
```

需要 `.env` 里有 `LLM_API_KEY`。会真跑 T1/T2 两个主线任务并逐条比对黄金答案。

### 看它到底怎么想的

任何一次运行后：

```bash
py -3.11 -c "import json;[print(json.loads(l).get('step'), json.loads(l).get('tool'), json.loads(l).get('result_summary','')) for l in open('trace.jsonl',encoding='utf-8')]"
```

`trace.jsonl` 每步一行，工具名、参数、结果摘要全在。
面试演示时这个比 UI 更有说服力 —— 它证明决策是模型做的，不是代码写死的。

---

## 6. 已知限制与后续要排查的点

诚实清单。面试时主动说这些，比被问出来好。

### 6.1 部署相关（**最需要你留意**）

| 问题 | 说明 | 怎么办 |
|---|---|---|
| **自定义域走了 Cloudflare 代理** | `agent.llynb.cc` 的响应头带 `CF-RAY` / `Server: cloudflare`，而 Railway 原域名是 `Server: railway-hikari` —— 说明流量经过 CF 代理层。代码里的 `X-Accel-Buffering: no` 是 nginx 指令，**Cloudflare 不认** | 演示前跑一个任务，确认 trace 是**逐步刷出**而不是转很久后一次性出现。若被缓冲，把 Cloudflare 该记录改成灰云（DNS only）即可绕开 |
| **Railway 文件系统是临时的** | 容器重启/重新部署后 `.index/index.db` **会丢**，需要重新 sync | 演示前现场 sync 一次即可（种子语料只要 0.44s）；要持久化得挂 Railway Volume |
| **Web 端注册的是「服务器上的路径」** | 远程用户在网页里填 `D:/公司资料` 一定失败 —— 那是**你**电脑上的路径，服务器看不到 | 线上演示只注册容器内路径（如 `workspace_seed`）；「索引我自己的文件夹」这个能力请用本地 CLI 演示 |
| **上传的文件也存在临时磁盘上** | 容器重启后，上传的文件夹和索引一起消失 | 演示前现场传一次即可；要长期保留需挂 Railway Volume |
| **视觉解析要花钱** | 每页一次模型调用。默认关闭，`VISION_MAX_PAGES` 限制单文档页数 | 别对着几百页扫描件开着跑，先小批量试 |

### 6.2 功能限制

| 限制 | 影响 | 备注 |
|---|---|---|
| 检索无语义泛化 | 搜「采购」召不回「购买」 | 词法检索的固有代价；RRF 框架已就位，要补就再加一路 embedding |
| `substr` 路是全表扫描 | 十万块级别会到百毫秒 | 当前体量无感；真要扩容需换中文分词或 n-gram 倒排 |
| 索引要手动 `sync` | 改了文件不同步就查不到新内容 | 但**陈旧证据会被复核剔除**，不会因此给出错误答案 |
| Web 模式下证据只有块、读不到全文 | 网页里 agent 的 `read_file` 只能看会话工作区，看不到索引目录原文 | 这是刻意的隔离；要核对全文请用 CLI `ask` |
| 单机 SQLite | 多实例无法共享索引 | 要扩就换 Postgres + pg_trgm |
| 无并行工具调用 | 多文件读取串行，慢 | 早就在已知不足清单里 |
| 无产物自检回路 | 写完索引不会自己复核每条是否成立 | **我认为这是最该补的一件事** |

### 6.3 如果出问题，先看这里

| 症状 | 大概率原因 | 处理 |
|---|---|---|
| `index sync` 报某些文件失败 | 缺解析依赖，或文件本身损坏/加密 | `index status` 会列出失败文件与原因；缺依赖就装 `requirements.txt` |
| 中文搜不到 | 先用 `index search` 直接查，绕开模型 | 若 `search` 有结果而 `ask` 说没有，是模型环节；反之是索引环节 |
| 搜出来的内容和文件对不上 | 索引陈旧 | 跑 `index sync`；系统本身也会复核并剔除过期证据 |
| 结局是 `DEGRADED` | 步数或 token 触顶 | **不等于失败**，已落盘产物全部保留；可调 `AGENT_MAX_STEPS` / `AGENT_TOKEN_BUDGET` |
| pip 装依赖报 UnicodeDecodeError | `requirements.txt` 混入了非 ASCII 字符 | 该文件必须保持纯 ASCII（Windows 下 pip 用 GBK 读它） |
| 线上 Demo 报未授权 | 访问口令 | 在 Railway 环境变量里看 `DEMO_ACCESS_CODE` |

---

## 7. 交付前必做（只能你来）

1. ~~仓库转 Public~~ ✅ 已完成

1.5 **Railway 环境变量**（在服务的 Variables 标签，Raw Editor 批量粘贴最快）：

   ```bash
   QWEN_API_KEY=<你的千问 key>
   QWEN_MODEL=qwen-vl-max
   VISION_OCR=1
   VISION_PROVIDER=qwen
   VISION_MODEL=qwen-vl-max
   VISION_MAX_PAGES=12
   DEMO_MAX_TOKENS_PER_TASK=150000
   ```

   ⚠️ 最后一条要特别注意：**环境变量优先于代码默认值**。
   如果线上已经设过 `DEMO_MAX_TOKENS_PER_TASK=80000`，光更新代码不生效，必须改这个变量。
   ⚠️ `QWEN_MODEL` 不填会用内置缺省 `qwen-max` —— 那是**纯文本模型，读不了图**。

2. **发提交邮件**给 `（收件人已脱敏）`，内容含：
   - 仓库地址 https://github.com/lilongyong333/workspace-agent
   - 在线 Demo 地址 + 访问口令
   - 一句话说明：手写循环、无 agent 框架、支持任意文件夹检索

另外两件建议做的：

3. **轮换两把 API key** —— DeepSeek 与通义千问的 key 都在我们的聊天记录里出现过，交付后各换一把。
4. **确认 Railway 重新部署成功** —— push 后打开线上页面跑一个示例任务。

---

## 8. 面试时值得主动讲的三点

不要只讲「我实现了什么」，讲**为什么这么选**。

1. **最强的安全边界是「能力不存在」。**
   工具集里没有删除能力，工作区里两处注入都要求删文件 ——
   无论模型被说服到什么程度，攻击都不可能成功。
   只读模式是同一条思路的延伸：模型调用不了一个它看不见的工具。

2. **用接口形状引导模型，而不是用提示词哀求。**
   `read_file` 的签名里没有「读全文」这个选项；`search` 的返回体大小
   与被搜文件大小无关。面对 12,000 行日志，模型自己就转向了 `search`。
   *提示词会在上下文压力下被忽略，接口不会。*

3. **加索引引入了新风险，不是纯收益。**
   索引是快照，会陈旧，会让模型输出「看起来有出处的错误答案」。
   所以每条证据进上下文前都要复核哈希。
   能主动说出「我引入的这个方案带来了什么新问题、我怎么处理的」，
   比多写三个功能有用。
